apiVersion: v1
kind: ConfigMap
metadata:
  name: nexaflow-api-config
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $value := .Values.api.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
